import type UseDisableSelectionModeOnBackCallback from './type';

// On native platforms other than Android there is no back button that should dismiss selection mode.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function useDisableSelectionModeOnBack(_callback: UseDisableSelectionModeOnBackCallback) {}
